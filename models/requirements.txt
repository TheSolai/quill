# Quill model layer
# Core dependencies for the model orchestration layer.

# Backend (FastAPI/Flask) — used by Quill
flask>=3.0.0
flask-cors>=4.0.0
ollama>=0.1.0

# Model config loader
pyyaml>=6.0

# Testing
pytest>=8.0.0
requests>=2.31.0
